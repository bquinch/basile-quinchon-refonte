const scrolling = () => {

  const elements = document.querySelectorAll('.anchor')
  document.addEventListener('wheel', (event) => {
    // TODO : Scroll to next element with the anchor class in the view
  })
};

export { scrolling };
